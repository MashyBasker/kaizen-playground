> Note: Needs kaizen installed

Command for running the example:

Modify the source and output path in `rust_testgen.py` before running

```console
PYTHONPATH=. poetry run python examples.temp/rust_testgen.py
```


